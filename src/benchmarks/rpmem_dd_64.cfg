# Global parameters
[global]
group = rpmem
file = /home/nesperf/poolset-dir/poolset.dd
ops-per-thread = 100000

[rpmem_persist_DS64]
bench = rpmem_persist
threads = 24
data-size = 64
mem-mode = seq-wrap
